/* eslint-disable @typescript-eslint/no-explicit-any */

const WaitingForSelection = ({ data }: any) => {
    console.log(data);
    return (
        <div>
            WaitingForSelection
        </div>
    );
};

export default WaitingForSelection;